<ul class="list-group">
  <?php if ($channels->totalCount()) foreach ($channels as $channel) : ?>
  <li class="list-group-item p-3">
    <div class="pl-3 d-flex justify-content-between" style="border-left: 4px solid #555">
      <a href="<?= H($channel->url) ?>" class="text-dark"><?= H($channel->name) ?></a>
      <span>></span>
    </div>
  </li>
  <?php endforeach; ?>
</ul>