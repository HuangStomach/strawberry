<div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel"><?= T('删除用户') ?></h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <?= T("您确定删除用户{$user->name}吗") ?>
        <form id="userDelete" method="post" action="user/delete">
          <input type="hidden" name="id" value="<?= $user->id ?>">
        </form>
        <br/>
        <div class="text-right">
          <button type="button" class="btn btn-sm btn-secondary" data-dismiss="modal"><?= T('关闭') ?></button>
          <button type="submit" class="btn btn-sm btn-danger" form="userDelete"><?= T('删除') ?></button>
        </div>
      </div>
    </div>
  </div>
</div>

<script data-ajax>
require(['main'], function ($) {  
  if ($('.modal').length > 0) {
    $(".modal:not(':eq(0)')").remove()
  }
  $('.modal').modal('show')

  $('.modal').on('hidden.bs.modal', function (e) {
    $('.modal').remove()
  })
})
</script>